[workspace]
resolver = "2"
members = [
    "host",
    "test",
    "examples/dstack-prove",
]

# Release profile — used by the host binary
[profile.release]
opt-level = 3
debug = false
lto = true
codegen-units = 1
panic = 'abort'
overflow-checks = true
